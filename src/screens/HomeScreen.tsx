import React from 'react';
import {View} from "react-native";
import {SwipeCards} from "../features/ItemListing/Components/SwipeCards";

export const HomeScreen = () => {
    return (
        <View>
            <SwipeCards/>
        </View>
    );
};

